1. Write a program that accepts the input of values from the user and prints it on the screen.
#include<stdio.h>
#include<conio.h>
void main()
{
  int i,a[4];
  clrscr();

  for(i=1;i<=1;i++)
   {
   printf("enter array");
   scanf("%d",&a[i]);
   }
   for(i=1;i<=1;i++)
   {
   printf("%d",a[i]);
   }
   getch();
}