9.	Write a program to check whether the given number is prime or not. 
#include<stdio.h>
#include<conio.h>

void main()
{
	int a,b,temp=0;
	clrscr();

	printf("enter prime number:");
	scanf("%d",&b);

	for(a=2;a<=b/2;a++)
	{
	if(b %a==0)
		{
		temp++;
		break;
		}

	}
	if(temp==0&&b!=1)
	{
	printf("%d is a prime",b);
	}
	else
	{
	printf("%d not prime",b);
	}
	getch();
}