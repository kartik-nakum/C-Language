8.	Write a program to generate the Fibonacci series up to a given number using a for loop.
#include<stdio.h>
#include<conio.h>

void main()
{
	int a=0,b=1,c,i,n;
	clrscr();

	printf("enter the length of the fibonacci series \n");
	scanf("%d",&n);
	printf("the fibonacci series is :\n");
	printf("%d%d" ,a,b);

	for(i=2;i<=n;i++)
	{
		c=a+b;
		printf("%d",c);
		a=b;
		b=c;

	}
	getch();
}